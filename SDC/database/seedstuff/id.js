const faker = require('faker');

const productId = (M) => {
  const l = Math.floor(M * .75);
    for (let i = 0; i <= l; i++) {
      let result = 0++;
    }
    return result;
  }

  module.exports = productId;